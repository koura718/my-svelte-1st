/** @type {import('./$types').Actions} */
export const actions = {
    default: async (event) => {
        // TODO log the user in
        throw error(400, 'Cannot access OpenAI API Service.');

    }
};